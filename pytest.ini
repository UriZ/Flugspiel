[pytest]
testpaths = tests
# -ra: always list skips. A silently skipped test reads as coverage it is not (#10).
addopts = -ra
pythonpath = .
markers =
    realdata: requires the built connectome in the data directory
